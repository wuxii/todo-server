package com.harmony.todo.repository;

import com.harmony.todo.core.repository.QueryableRepository;
import com.harmony.todo.domain.UserAccount;

public interface UserAccountRepository extends QueryableRepository<UserAccount> {
}
